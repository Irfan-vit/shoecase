import styled from 'styled-components'

const StyledAddress = styled.form`
  max-width: 500px;
  margin: 0 auto;
  display: flex;
  flex-flow: row wrap;
  > label {
    flex
    /* display: block; */
  }
`
export { StyledAddress }
