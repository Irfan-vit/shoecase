import styled from 'styled-components'

const StyledAddress = styled.form`
  max-width: 1000px;
  margin: 0 auto;
  display: flex;
  > label {
    /* display: block; */
  }
`
export { StyledAddress }
