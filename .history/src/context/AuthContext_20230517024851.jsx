import { createContext, useContext } from 'react'

const AuthContext = createContext()

const AuthProvider = () => {
    const [token , setTocken] = use
  return <AuthContext.Provider value={{}}></AuthContext.Provider>
}
